#!/usr/bin/python3 -u
import sys
import os

cwd = os.getcwd()

while True:
    line = sys.stdin.readline()
    if line == '':
        sys.exit(0)
    if cwd in line and not 'node_modules' in line:
        line = '\033[32m\033[1m' + line + '\033[0m'
    sys.stdout.write(line)
